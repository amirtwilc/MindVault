<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.USE_BIOMETRIC"/>
    <uses-permission android:name="android.permission.USE_FINGERPRINT"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>

    <application
        android:label="MindVault"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="false"
        android:supportsRtl="true"
        android:enableOnBackInvokedCallback="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"/>
            <meta-data
              android:name="flutter_deeplinking_enabled"
              android:value="false"/>

            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Deep link for Supabase OAuth callback -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data
                    android:scheme="mindvault"
                    android:host="auth"
                    android:path="/callback"/>
            </intent-filter>

            <!-- Deep link for reminder notification taps -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data
                    android:scheme="mindvault"
                    android:host="reminder"
                    android:path="/note"/>
            </intent-filter>
        </activity>

        <!-- Transparent Flutter activity launched by the widget "New Note" button -->
        <activity
            android:name=".TransparentActivity"
            android:exported="true"
            android:theme="@style/TransparentTheme"
            android:launchMode="singleTop"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode">
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="mindvault" android:host="widget"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".ReminderAlarmActivity"
            android:exported="false"
            android:theme="@style/ReminderAlarmTheme"
            android:excludeFromRecents="true"
            android:finishOnTaskLaunch="true"
            android:launchMode="singleTop"
            android:noHistory="true"
            android:process=":reminder"
            android:taskAffinity=""/>

        <!-- Home widget: list data service (feeds the scrollable ListView) -->
        <service
            android:name=".NoteWidgetService"
            android:exported="false"
            android:permission="android.permission.BIND_REMOTEVIEWS"/>

        <!-- Home widget provider -->
        <receiver
            android:name=".HomeWidgetProvider"
            android:exported="true"
            android:label="@string/widget_notes_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/mindvault_widget_info"/>
        </receiver>

        <!-- Categories widget: list data service -->
        <service
            android:name=".CategoryWidgetService"
            android:exported="false"
            android:permission="android.permission.BIND_REMOTEVIEWS"/>

        <!-- Categories widget provider -->
        <receiver
            android:name=".CategoriesWidgetProvider"
            android:exported="true"
            android:label="@string/widget_categories_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/categories_widget_info"/>
        </receiver>

        <receiver
            android:name=".ReminderAlarmReceiver"
            android:exported="false"
            android:process=":reminder"/>

        <receiver
            android:name=".ReminderBootReceiver"
            android:exported="false"
            android:process=":reminder">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
            </intent-filter>
        </receiver>

        <receiver
            android:name=".ReminderReconcileReceiver"
            android:exported="false"
            android:process=":reminder"/>

        <service
            android:name=".ReminderAlarmService"
            android:exported="false"
            android:foregroundServiceType="shortService"
            android:process=":reminder"/>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2"/>
    </application>
</manifest>
